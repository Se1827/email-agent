33: /* ─── small helpers ─────────────────────────────────────────────────────── */\n99: {live ? 'Live · Microsoft Graph' : 'Mock Mode'}\n108: <span>Loading…</span>\n123: /* ─── Mail Tab ───────────────────────────────────────────────────────────── */\n382: <textarea className="input" rows={10} placeholder="Message body…" value={form.body}\n386: {sending ? 'Sending…' : 'Send Email'}\n402: {/* ── Action row (above body so always visible) ── */}\n436: {/* ── Email Thread ────────────────────────────────── */}\n474: {/* ── AI results panel ───────────────────────────── */}\n506: <div className="ol-cal-event-title">📅 {aiCalEvent.title}</div>\n509: {aiCalEvent.end && ` → ${new Date(aiCalEvent.end).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })}`}\n532: /* ─── Calendar Tab ───────────────────────────────────────────────────────── */\n578: showToast('✅ Event created!');\n582: } catch (e) { showToast(`❌ ${e.message}`, 'error'); }\n622: {saving ? 'Creating…' : 'Create Event'}\n645: {start ? new Date(start).toLocaleDateString([], { weekday: 'short', month: 'short', day: 'numeric' }) + ' · ' + new Date(start).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' }) : '—'}\n646: {end ? ` → ${new Date(end).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })}` : ''}\n649: <div className="ol-event-location">📍 {ev.location.displayName}</div>\n670: /* ─── Contacts Tab ───────────────────────────────────────────────────────── */\n701: <input className="ol-search-input" placeholder="Search contacts…" value={search}\n731: /* ─── Files Tab ──────────────────────────────────────────────────────────── */\n748: const icon = item => item.folder ? '📁' : '📄';\n776: {item.size && ` · ${size(item.size)}`}\n792: /* ─── Profile Tab ────────────────────────────────────────────────────────── */\n837: /* ─── Groups Tab ─────────────────────────────────────────────────────────── */\n882: /* ─── Teams Tab ──────────────────────────────────────────────────────────── */\n934: <textarea className="input" rows={4} placeholder="Message…"\n939: {sending ? 'Sending…' : 'Send to Teams'}\n958: {presence.activity && <span className="ol-presence-activity">· {presence.activity}</span>}\n966: /* ─── Main OutlookPage ───────────────────────────────────────────────────── */\n998: <p className="ol-subtitle">Outlook · Teams · OneDrive · Contacts</p>\n